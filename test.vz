import std::io;
import std::crypto;

public module Demo {

  public class Demo {

    public mthd GenName(): String {
      rv = String();
      for k = 0; k < 12; k++ {
        rv = rv + Rand.Next();
      }
      return rv;
    }

  }

}
